# add_library(osg SHARED IMPORTED GLOBAL)
# # file(GLOB_RECURSE OSG_DLL "${CMAKE_CURRENT_SOURCE_DIR}/bin/*.dll")
# # file(GLOB_RECURSE OSG_LIB "${CMAKE_CURRENT_SOURCE_DIR}/lib/*.lib")

# foreach(idx ${OSG_DLL})
#   set_property(TARGET osg PROPERTY IMPORTED_LOCATION ${idx})
  
# endforeach()
# foreach(idx ${OSG_LIB})
#   set_property(TARGET osg PROPERTY IMPORTED_IMPLIB ${idx})
# endforeach()

# # set_property(TARGET osg PROPERTY IMPORTED_LOCATION ${OSG_DLL})
# set_property(TARGET osg PROPERTY IMPORTED_IMPLIB ${OSG_LIB})

install(FILES ${OSG_DLL} DESTINATION ${BINARY_ROOT_DIR})
set(OSG_NOTIFY_LEVEL DEBUG_INFO)
# set(OPENGL_PROFILE GLCORE)
# set(GLCORE_GLCOREARB_HEADER ${CMAKE_CURRENT_SOURCE_DIR}/include/glcore)
# set(OSG_GL3_AVAILABLE on)
# set(OSG_GL1_AVAILABLE off)
# set(OSG_GL2_AVAILABLE off)
# set(OSG_GLES1_AVAILABLE off)
# set(OSG_GLES2_AVAILABLE off)
# set(OSG_GL_DISPLAYLISTS_AVAILABLE off)
# set(OSG_GL_FIXED_FUNCTION_AVAILABLE off)
# set(OSG_GL_MATRICES_AVAILABLE off)
# set(OSG_GL_VERTEX_ARRAY_FUNCS_AVAILABLE off)
# set(OSG_GL_VERTEX_FUNCS_AVAILABLE off)
# target_include_directories(osg INTERFACE ${OSG_INC_PATH})