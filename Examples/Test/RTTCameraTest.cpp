void bindRTTCamera(){
    // const std::string& vertPath = (Core::g_runtimeContext.configManager->getShaderFolder() / "globalEffects/screen.vert").string();
    // const std::string& fragPath = (Core::g_runtimeContext.configManager->getShaderFolder() / "globalEffects/screen.frag").string();
    // RTTCamera* rttCamera = new RTTCamera(m_mainColorTexture,m_captureImage,vertPath,fragPath);
    // rttCamera->getRTTCamera()->setPreDrawCallback(m_captureCallback);
    // rttCamera->getRTTCamera()->setRenderOrder(osg::Camera::PRE_RENDER,0);
    // rttCamera->getRTTCamera()->setClearColor(osg::Vec4f(0,0,0,0));
    // osg::Group* sceneRoot =  dynamic_cast<osg::Group*>(viewer->getSceneData());
    // sceneRoot->addChild(rttCamera->getRTTCamera());
}